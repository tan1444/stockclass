// had enabled by midway
// export = {
//   static: true,
// };
export const jwt = {
  enable: true,
  package: 'egg-jwt'
};

export const redis = {
  enable: true,
  package: 'egg-redis',
}

export const view = {
  enable: true,
  package: 'egg-view',
};

export const ejs = {
  enable: true,
  package: 'egg-view-ejs',
};

export const swaggerdoc = {
  enable: true,
  package: 'egg-swagger-doc',
};